<%args>
</%args>

<div class="gene-update">
  <% $c->stash->{form}->render() %>

% if (0 && @unknown_identifiers) {
  <div class="unknown_identifiers">
    
%   for my $identifier (@unknown_identifiers) {
    <span> <% $identifier %> </span>
%   }
  </div>
% }
</div>

<%init>
my @unknown_identifiers = ();

my $stash_unknown = $c->stash()->{gene_upload_unknown};

if ($stash_unknown) {
  @unknown_identifiers = @{$stash_unknown};
}
</%init>
